import { ScrollProductsDirective } from './scroll-products.directive';

describe('ScrollProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollProductsDirective();
    expect(directive).toBeTruthy();
  });
});
